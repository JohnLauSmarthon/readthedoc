version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"

# Add this specific section to fix the error
sphinx:
  configuration: conf.py

python:
  install:
    - requirements: requirements.txt
